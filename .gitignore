# Captured data and its token never belong in the repo.
link-captures.jsonl
*.jsonl
*.token

# This repo's own working data — the built-in default for DATA_DIR.
data/

__pycache__/
*.pyc

# Built add-on packages
*.xpi
web-ext-artifacts/

.DS_Store

# Machine-specific paths.
config.local.sh
link-handoff.json

# Android build output
android/build/
.amo-upload-uuid

# Page preview build output; holds captured text.
tools/preview-pages/out/
